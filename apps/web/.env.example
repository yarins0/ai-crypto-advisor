# apps/web — copy to .env and fill in. Never commit .env.
# Vite only exposes variables prefixed with VITE_ to browser code.

# Base URL of the API. In production this is the Render URL, for example
# https://ai-crypto-advisor-api.onrender.com
VITE_API_URL=http://localhost:4000
